{
  "name": "housing-monorepo",
  "private": true,
  "packageManager": "pnpm@9.11.0",
  "workspaces": ["web", "server", "lex"],
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm -r dev",
    "typecheck": "pnpm -r typecheck",
    "lint": "pnpm -r lint",
    "format": "prettier -w ."
  },
  "engines": {
    "node": ">=20"
  }
}
